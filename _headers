# Sohadot.com — Security Headers
# Works with: Cloudflare Pages, Netlify, or any CDN that supports _headers files
# For GitHub Pages: add these via a Cloudflare proxy (free plan)

/*
  # Prevent clickjacking
  X-Frame-Options: SAMEORIGIN

  # Prevent MIME sniffing
  X-Content-Type-Options: nosniff

  # XSS protection (legacy browsers)
  X-XSS-Protection: 1; mode=block

  # Referrer policy — privacy-friendly
  Referrer-Policy: strict-origin-when-cross-origin

  # Permissions policy — disable unnecessary browser features
  Permissions-Policy: camera=(), microphone=(), geolocation=(), payment=()

  # Content Security Policy
  # Allows: self, Google Fonts, WhatsApp links, Anthropic API (for valuation AI mode)
  Content-Security-Policy: default-src 'self'; script-src 'self' 'unsafe-inline'; style-src 'self' 'unsafe-inline' https://fonts.googleapis.com; font-src 'self' https://fonts.gstatic.com; connect-src 'self' https://api.anthropic.com; img-src 'self' data: https:; frame-ancestors 'none';

  # HTTPS upgrade
  Strict-Transport-Security: max-age=31536000; includeSubDomains; preload
